// Compat shim — re-exports the new shell. Delete after all pages migrate to
// the direct import: `@/components/admin/AdminLayout`.
export { AdminLayout as default } from "@/components/admin/AdminLayout";
export { AdminLayout } from "@/components/admin/AdminLayout";
